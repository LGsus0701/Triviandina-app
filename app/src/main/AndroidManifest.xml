<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/trivia"
        android:label="TriviAndina"
        android:roundIcon="@drawable/trivia"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tandina"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <activity
            android:name=".presentacion.inicio.preguntas.PreguntasActivity"
            android:exported="false" />
        <activity
            android:name=".presentacion.inicio.categorias.CategoriasActivity"
            android:exported="false" />
        <activity
            android:name=".presentacion.inicio.InicioActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>